#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    while(scanf("%d",&n) != EOF)
    {
        if(n==0)
            cout<<"vai ter copa!\n";
        else

        printf("vai ter duas!\n");
    }

    return 0;
}
